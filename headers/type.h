#ifndef TYPE_H
#define TYPE_H

#include <string>

enum Types { Water, Earth, Air, Fire, Dark, Light, Thunder, Metal };

enum Special_Types { Psychic, Colour, Hell_Fire };

Types getRandomType();

#endif  // TYPE_H
